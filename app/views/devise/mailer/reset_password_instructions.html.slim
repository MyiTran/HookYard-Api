h2 Reset your HookYard password

p Hello #{@resource.email}

p Click the link below to create a new password.

= link_to 'Change my password', "#{ENV.fetch('FRONTEND_URL')}/users/password/edit?reset_password_token=#{ERB::Util.url_encode(@token)}"

p If you did not request this, please ignore this email.
